# 写作规范（供各章节作者遵循）

本书是《DeepSeek Harness 源码解读》，目标读者是正在学习 Agent Harness 工程实现的工程师。全书的灵魂是：**从真实源码中提炼工程上的优雅之处**，不是复述文档。

## 源码位置

- 仓库克隆在：`/private/tmp/claude-503/-Users-lxc-Agents-trend-researcher/8a3289ce-88c6-45f3-9134-52907913df32/scratchpad/deepseek-harness`
- 仓库自带高质量文档（`docs/`、各包 `README.md`、`docs/subsystems/*.md`），可以作为地图，但**必须回到 `src/*.ts` 源码求证**，章节内容以源码为准。

## 每章结构（必须遵循）

1. **开篇一段话**：这一章解决什么问题、为什么值得读（不超过 5 句）。
2. **问题背景**：如果你自己实现这个子系统，朴素做法是什么、会遇到什么坑。
3. **源码剖析**：带真实代码摘录的讲解。
   - 代码摘录必须来自真实源码，注明文件路径（相对仓库根，如 `packages/core/agent-loop/src/index.ts`），可以为篇幅做删节但要用 `// ...` 标注。
   - 每段摘录后面必须有讲解，禁止贴大段代码不解释。
   - 单章代码摘录总量控制在 80-150 行之间。
4. **设计亮点**：用 `> 💎 **设计亮点**` 引用块标出 2-4 处工程优雅之处，说清楚「换一种普通写法会怎样、这里妙在哪」。
5. **小结与延伸**：3-5 句总结 + 指向仓库内相关文件/文档的阅读清单。

## 行文要求

- 全文中文，技术术语、代码标识符保留英文原文（如 waterfall、`ctx.effect()`）。
- 每章正文 150~300 行 markdown（不含代码块），宁可深不可泛。
- 语气：有经验的工程师带读，直接、具体，避免空洞的形容词堆砌。
- 禁止编造：所有 API 名、文件路径、行为描述都必须在源码中验证过。不确定的内容宁可不写。
- Mermaid 图：涉及流程/依赖时鼓励画 mermaid 图（每章至多 2 张）。
- 章与章之间用相对链接互相引用（如 `[第 5 章](../part2/05-typed-events.md)`）。

## 「优雅」的判断标准

优先挖掘这几类素材：

- **用类型系统消灭一类错误**（declaration merging、typert、branded types）
- **用一个原语统一多种场景**（waterfall 同时做策略/改写/短路；seam 三角让 provider 可整体替换）
- **可逆性设计**（effect/disposer、append-only log、patch 分层）
- **不变量与防御**（runtime invariant、"model-visible means logged"）
- **让错误无处藏身**（错误恢复路径、审批流水线）
- **工程化自动化**（生成式文档、事件目录校验、i18n 流水线）
